# NextAuth v5 필수 환경변수 (openssl rand -base64 32 으로 생성)
AUTH_SECRET=""

# 앱 URL (프로덕션에서 실제 URL로 교체)
NEXTAUTH_URL=http://localhost:3000

# Notion CMS 설정
NOTION_API_KEY=          # Notion Integration 토큰 (https://www.notion.so/my-integrations)
NOTION_DATABASE_ID=      # 블로그 DB ID (URL에서 추출)
REVALIDATE_SECRET=       # ISR 재검증 시크릿 (openssl rand -base64 32 으로 생성)
